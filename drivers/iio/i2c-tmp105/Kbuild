# SPDX-License-Identifier: GPL-2.0
#
# iio_i2c_tmp105：TMP105 的 IIO 子系统驱动（对照 hwmon_i2c_tmp105：同一颗芯片两种外壳）
#
# 命名规则见 doc/开发规范.md：
#   目录 drivers/<子系统>/<总线>-<器件>/，模块名 = 主文件名 = <子系统>_<器件>
#   （这里 <子系统>=iio、<总线>=i2c、<器件>=tmp105；叶目录 i2c-tmp105
#    只描述"总线+器件"，子系统由父目录给出；模块名带子系统保证全局唯一）
#
# 注意：本模块与 hwmon_i2c_tmp105 都匹配 ti,tmp105@0x48，同一时刻只能
# 加载一个（i2c 核心一个从机地址只允许一个 client 驱动绑定，
# 见 doc/开发规范.md §8.1）；chardev_i2c_tmp105 不占地址，可与任一共存。
obj-m := iio_i2c_tmp105.o